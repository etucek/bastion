title: Post
extends@: default

form:
  fields:
    tabs:
      fields:

        post:
          type: tab
          title: THEME_BASTION.BLUEPRINTS.POST
          ordering@: 2

          fields:

            blog_layout_section:
              type: section
              title: THEME_BASTION.BLUEPRINTS.LAYOUT_CONFIGURATION
              underline: true
              ordering@: 1

              import@:
                type: partials/blog

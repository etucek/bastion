{% extends 'partials/langswitcher-logic.html.twig' %}

{% block language_block_start %}
<div class="relative" data-dropdown>
    <button type="button" data-dropdown-toggle aria-haspopup="listbox" aria-expanded="false"
            aria-label="{{ 'THEME_BASTION.LANGUAGES'|t|e('html_attr') }}"
            class="flex h-9 items-center gap-1 rounded-md px-2 text-sm text-gray-600 hover:text-[var(--accent)] dark:text-gray-300">
        <i class="fa-solid fa-globe" aria-hidden="true"></i>
        <span class="uppercase">{{ langswitcher.current }}</span>
        <i class="fa-solid fa-chevron-down text-[0.65rem] text-gray-500 dark:text-gray-400" aria-hidden="true"></i>
    </button>
    <ul data-dropdown-panel role="listbox" aria-label="{{ 'THEME_BASTION.LANGUAGES'|t|e('html_attr') }}"
        class="hidden absolute right-0 top-full z-30 mt-1.5 min-w-32 rounded-md border border-gray-300 bg-white py-1 text-sm shadow-lg dark:border-gray-600 dark:bg-gray-800">
{% endblock %}

{% block language_block_end %}
    </ul>
</div>
{% endblock %}

{% block language_item %}
    {% if show_language %}
    <li role="option" aria-selected="{{ active_class == 'active' ? 'true' : 'false' }}">
        <a href="{{ lang_url ~ uri.params ~ (uri.query|length > 1 ? '?' ~ uri.query) }}"
           class="block px-3 py-1.5 transition {{ active_class == 'active' ? 'font-medium text-[var(--accent)]' : 'text-gray-700 hover:bg-gray-100 dark:text-gray-200 dark:hover:bg-gray-700' }}">
            {{ native_name(language)|capitalize }}
        </a>
    </li>
    {% endif %}
{% endblock %}
